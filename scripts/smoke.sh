#!/usr/bin/env bash
# End-to-end smoke test — Phase 0.8.
# TODO(phase 0.8): implement per docs/05-claude-code-brief.md §0.8:
#   1. POST 100 events with varied lat/lon to ingest-api
#   2. sleep 90 (window close + checkpoint cushion)
#   3. GET /zones/hot?limit=10
#   4. Assert non-empty
set -euo pipefail

echo "smoke test not yet implemented — see Phase 0.8"
exit 1
